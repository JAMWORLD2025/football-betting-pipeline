# Copy this to .env for local testing. On Railway, set these as service
# variables in the dashboard instead — never commit a real .env file.

API_FOOTBALL_KEY=your_api_football_key_here
MONGODB_URI=your_mongodb_connection_string_here
MONGODB_DB_NAME=football_betting
PORT=3000
